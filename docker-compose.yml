version: '3.9'
services:
  main:
    container_name: main
    build:
      context: ./auth_feature/
      dockerfile: ./Dockerfile
    env_file:
      - ./.env
    ports:
      - 5000:5000
    command: python3 main.py
    volumes:
      - ./auth_feature:/code